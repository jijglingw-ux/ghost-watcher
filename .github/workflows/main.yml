name: Phoenix Watchdog

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run-watchdog:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install supabase cryptography

      - name: Run Watchdog Script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          RSA_PRIVATE_KEY: ${{ secrets.RSA_PRIVATE_KEY }}
          
          # 👇 关键修改：左边是Python要的名字，右边是您GitHub后台实际的名字
          EMAIL_USER: ${{ secrets.SENDER_EMAIL }}      # 对应您截图里的 SENDER_EMAIL
          EMAIL_PASS: ${{ secrets.SENDER_PASSWORD }}   # 对应您截图里的 SENDER_PASSWORD
        
        run: python watchdog.py
